<%= form_for(@pnm) do |f| %>
  <% if @pnm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pnm.errors.count, "error") %> prohibited this pnm from being saved:</h2>

      <ul>
      <% @pnm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
